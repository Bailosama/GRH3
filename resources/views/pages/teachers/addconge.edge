<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ajouter un congé</title>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">

  <!-- Navbar -->
  <nav class="bg-white shadow-md p-4 flex justify-between items-center">
    <div class="flex items-center">
      <!-- Logo -->
      <a href="{{ route('dashbord.dashbord') }}" class="text-xl font-bold text-blue-500">Logo</a>
    </div>

    <!-- Right section: user profile and icons -->
    <div class="flex items-center space-x-4">
      <button class="flex items-center space-x-2">
        <span class="text-gray-700">admin mr</span>
        <img src="path/to/your/avatar.jpg" alt="Avatar" class="w-8 h-8 rounded-full">
      </button>
      <div class="hidden group-hover:block absolute right-0 mt-2 w-48 bg-white border rounded shadow-lg">
        <a href="#" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">Paramètres du Compte</a>
        <a href="#" class="block px-4 py-2 text-gray-700 hover:bg-gray-100">Déconnexion</a>
      </div>
    </div>
  </nav>

  <div class="flex">
    <!-- Sidebar -->
    <aside class="w-1/4 bg-gray-900 text-white h-screen p-4">
      <div class="flex flex-col items-center">
        <img src="path/to/your/avatar.jpg" alt="Avatar" class="w-16 h-16 rounded-full mb-4">
        <h2 class="text-xl font-semibold">admin mr</h2>
        <p class="text-gray-400">Administrateur</p>
      </div>
      <ul class="mt-6">
        <li class="mb-2"><a href="{{ route('dashbord.dashbord') }}"
            class="block py-2 px-4 hover:bg-gray-700 rounded">Tableau de bord</a></li>
        <li class="mb-2"><a href="{{ route('teachers.index') }}"
            class="block py-2 px-4 hover:bg-gray-700 rounded">Ajouter un enseignant</a></li>
        <li class="mb-2"><a href="/conges/add" class="block py-2 px-4 hover:bg-gray-700 rounded">Ajouter un congé</a>
        </li>
        <li class="mb-2"><a href="/etudiants/add" class="block py-2 px-4 hover:bg-gray-700 rounded">Ajouter un
            étudiant</a></li>
        <!-- Ajoutez d'autres liens selon vos besoins -->
      </ul>
    </aside>

    <!-- Main Content -->
    <main class="w-3/4 bg-white p-8 rounded-md shadow-md mx-auto mt-8">
      <h1 class="text-2xl font-bold mb-6 text-center">Ajouter un congé</h1>
      <form action="{{ route('post.store') }}" method="POST" class="space-y-4">
        {{ csrfField() }}
        <div class="grid grid-cols-2 gap-6">
          <div class="mb-4">
            <label for="nom" class="block text-gray-700 font-medium mb-1">Nom</label>
            <input type="text" id="nom" name="nom" class="form-input w-full border-gray-300 rounded-md shadow-sm"
              required>
          </div>
          <div class="mb-4">
            <label for="date_debut" class="block text-gray-700 font-medium mb-1">Date de début</label>
            <input type="date" id="date_debut" name="date_debut"
              class="form-input w-full border-gray-300 rounded-md shadow-sm" required>
          </div>
          <div class="mb-4">
            <label for="date_fin" class="block text-gray-700 font-medium mb-1">Date de fin</label>
            <input type="date" id="date_fin" name="date_fin"
              class="form-input w-full border-gray-300 rounded-md shadow-sm" required>
          </div>
          <div class="mb-4">
            <label for="motif" class="block text-gray-700 font-medium mb-1">Motif</label>
            <textarea id="motif" name="motif" rows="4" class="form-textarea w-full border-gray-300 rounded-md shadow-sm"
              required></textarea>
          </div>
          <div class="mb-4">
            <label for="duree" class="block text-gray-700 font-medium mb-1">Durée (en jours)</label>
            <input type="number" id="duree" name="duree" class="form-input w-full border-gray-300 rounded-md shadow-sm"
              required>
          </div>
          <div class="mb-4">
            <label for="etat" class="block text-gray-700 font-medium mb-1">État</label>
            <select id="etat" name="etat" class="form-select w-full border-gray-300 rounded-md shadow-sm" required>
              <option value="">-- Choisir un état --</option>
              <option value="en attente">En attente</option>
              <option value="accepté">Accepté</option>
              <option value="refusé">Refusé</option>
            </select>
          </div>
        </div>
        <button type="submit"
          class="bg-indigo-500 hover:bg-indigo-600 text-white font-medium py-2 px-4 rounded-md">Ajouter</button>
      </form>
    </main>
  </div>

</body>

</html>